# Ordered, Changable(vslues can change), Duplicates not Allowed because accessing bu keys if its there it will overwrite the exisitng value, Index starts from zero.
# Dictionary are key:value pair,Accessing dict is Using keys, values, like .key(), .values(), .items(provide keys:value pair tuple), .get(key)-to access specific value wihtout throw error
# Add new items: use dictname[key]=value, .update(key:value), 
# Remove old Items: pop(known key), popitem(randonly removes for unknown pairs),del keyword,pop(), clear()
#count(), copy(), reverse(), sort()
#Dictionary looping: keys(), values(), items()
